{{#if multiple}}
{{#power-select-multiple selected=selectedIngredients searchField="name" placeholder=placeholder options=filteredIngredients onchange=(action "changeIngredients") as |ingredient|}}
  {{ingredient.name}} {{ingredient.description}}
{{/power-select-multiple}}
{{else}}
  {{#power-select searchField="name" placeholder=placeholder options=filteredIngredients onchange=(action "changeIngredients") as |ingredient|}}
    {{ingredient.name}} {{ingredient.description}}
  {{/power-select}}
{{/if}}
