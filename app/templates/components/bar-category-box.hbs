<span>{{title}}</span>
<div>
  {{#each ingredients as |ingredient|}}
    <span class="ember-power-select-multiple-option">
      <span {{action "removeItem" ingredient}} class="ember-power-select-multiple-remove-btn">×</span>
      {{ingredient.name}}
    </span>
  {{/each}}
</div>
