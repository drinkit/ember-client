{{#if this.user.isAuthenticated}}
  <div class="mx-4 pt-[15px]">
    <IngredientChooser class="mb-[5px]" @multiple={{false}} @initSelectedIds={{this.selectedIngredientsIds}} @selectedIngredientsIds={{this.selectedIngredientsIds}} @ingredients={{this.ingredients}} @placeholder="Добавить в бар..." @ingredientSelected={{this.ingredientSelected}}/>
    <Hint @name="ingredientInBar" @text="Полезно знать: после добавления ингредиентов в бар, они будут отмечаться в результатах поиска с помощью серебряной рамки, даже если не учавствуют в фильтрах." />
    <div class="flex flex-wrap mt-[10px]">
      <div id="firstBarColumn" class="flex flex-col flex-1 w-full">
        <BarCategoryBox @title="Крепкие алкогольные напитки" class="ml-0" @ingredients={{this.ingredientsInCategories.hardSpirits}} @onRemoveItem={{action "itemDeleted"}} />
        <BarCategoryBox @title="Ликеры" class="ml-0" @ingredients={{this.ingredientsInCategories.liquors}} @onRemoveItem={{action "itemDeleted"}}/>
      </div>
      <div id="secondBarColumn" class="flex flex-col flex-1 w-full">
        <BarCategoryBox @title="Слабоалкогольные напитки" class="mr-0" @ingredients={{this.ingredientsInCategories.softSpirits}} @onRemoveItem={{action "itemDeleted"}} />
        <BarCategoryBox @title="Безалкогольные напитки" class="mr-0" @ingredients={{this.ingredientsInCategories.nonSpirits}} @onRemoveItem={{action "itemDeleted"}} />
        <BarCategoryBox @title="Прочее" class="mr-0" @ingredients={{this.ingredientsInCategories.other}} @onRemoveItem={{action "itemDeleted"}} />
      </div>
    </div>
  </div>
  <SuggestIngredients @suggestedIngredients={{this.suggestedIngredients}} @initialized={{this.suggestedIngredientsInitialized}}
                      @title="Если бы в вашем баре были следующие ингредиенты, вы бы смогли приготовить больше коктейлей:"
                      @noDataMode="warning" @noDataText="К сожалению, в вашем баре слишком мало ингредиентов, чтобы мы могли вам что-то посоветовать." />
  <SuggestIngredients @suggestedIngredients={{this.suggestedIngredients}} @initialized={{this.suggestedIngredientsInitialized}}
                      @noDataMode="hide" @title="Вам не хватает немного ингредиентов, чтобы приготовить ваши любимые коктейли:" />
{{else}}
  <span class="block text-center text-xl font-gothic mt-5">
    Чтобы воспользоваться баром, нужно <a class="text-blue-700 cursor-pointer hover:underline" {{on "click" (fn this.showLogin)}}>авторизироваться</a> на сайте.
  </span>
{{/if}}
