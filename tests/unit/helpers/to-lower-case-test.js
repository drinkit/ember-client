import { toLowerCase } from '../../../helpers/to-lower-case';
import { module, test } from 'qunit';

module('Unit | Helper | to lower case');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = toLowerCase(42);
  assert.ok(result);
});
