import { humanizeQuantity } from '../../../helpers/humanize-quantity';
import { module, test } from 'qunit';

module('Unit | Helper | humanize quantity');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = humanizeQuantity(42);
  assert.ok(result);
});
