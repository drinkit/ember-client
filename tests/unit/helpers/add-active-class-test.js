import { addActiveClass } from '../../../helpers/add-active-class';
import { module, test } from 'qunit';

module('Unit | Helper | add active class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = addActiveClass(42);
  assert.ok(result);
});
