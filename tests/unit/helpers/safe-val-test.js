import { safeVal } from 'ember-drink-it/helpers/safe-val';
import { module, test } from 'qunit';

module('Unit | Helper | safe val');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = safeVal([42]);
  assert.ok(result);
});
